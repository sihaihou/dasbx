def projects = "${projectNames}".split(",")
pipeline {
    agent any
    environment{
        harbor_url = '192.168.83.33:80'
        repo_name = 'dasbx'
        tag = 'latest'
    }
    stages {
        stage("test"){
            steps{
                script{
                    for(String aaa : projects){
                        sh 'echo ${aaa}'
                    }
                }
            }
        }
    }
}
